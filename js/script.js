/**
 * Arquivo: script.js
 * * Este é o local para adicionar todas as funcionalidades
 * interativas do seu site Vyrus Survivor (ex: efeitos de clique,
 * carregamento de dados, animações, etc.).
 */

document.addEventListener('DOMContentLoaded', () => {
    console.log("O site Vyrus Survivor carregou com sucesso!");

    // Exemplo de código: Adicionar uma classe "loaded" ao corpo
    // para iniciar animações CSS após o carregamento
    // document.body.classList.add('loaded');
});

// Adicione seu código JavaScript abaixo